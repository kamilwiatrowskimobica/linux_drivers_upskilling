// read waits for completion or until it times out
// write completes and read is done just after
// sudo dd ibs=1 count=1 if=/dev/chardevice0
// sudo bash -c "echo 'A' > /dev/chardevice0"

int main()
{}
